 @model IEnumerable<Model.StockInventoryModel> 

@using System.Web.UI.WebControls
@using POS.Areas.Admin.Controllers;

<h2>TreeViewDistrubuted</h2>

<link href="~/Content/jsTree/themes/default/style.min.css" rel="stylesheet" />
<div class="form-body">
    <div id="jstree">
        @*@(Html.TreeView(Model)
                          .EmptyContent("Distribution")
                          .Children(m => m.Childs)
                          .HtmlAttributes(new { id = "tree" })
                          .ChildrenHtmlAttributes(new { @class = "subItem" })
                          .ItemText(m => m.ProductName)
                          .ItemTemplate(
        
        @<text>
            <a href="@item.BranchList" desc="@item.BranchList">@item.ProductName</a>*@
        
            @*<a href="@Url.Action("BranchAdd", "StockInventory")">
                <i class="glyphicon glyphicon-home"></i> Branch Name
            </a>*@
        
        </text>
        )
    </div>
</div>
@section scripts
{
  
      <script src="~/Scripts/jsTree3/jstree.min.js"></script>
    <script>

        $(function () {
            var selectedData;
            $('#jstree').jstree({
                "core": {
                    "multiple": true,
                    "check_callback": false,
                    'themes': {
                        "responsive": true,
                        'variant': 'larg',
                        'stripes': false,
                        'dots': false
                    }
                },
                "types": {
                    "default": {
                        "icon": "fa fa-folder icon-state-warning icon-lg"
                    },
                    "file": {
                        "icon": "fa fa-file icon-state-warning icon-lg"
                    }
                },
                "plugins": ["dnd", "state", "types", "sort", "OnSelectedNodeChanged"]
            });
        });
    </script>
}   